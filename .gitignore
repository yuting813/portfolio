# node / build
node_modules/
dist/
.vite/
*.log

# workers / wrangler
.wrangler/
**/.wrangler/**
api/.wrangler/**
api/.dev.vars

# env
.env
.env.*
!.env.example

# 不要擋 npm 鎖檔（要提交）
# package-lock.json  ← 不要加到 ignore
# yarn.lock         ← 建議 ignore（此專案不用 yarn）
yarn.lock
